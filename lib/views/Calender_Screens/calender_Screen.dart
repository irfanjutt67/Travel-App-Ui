import '../../const/consts.dart';

class ClandarScreen extends StatelessWidget {
  const ClandarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Calender'));
  }
}
